-- todo_history.user_id 의 컬럼 코멘트만 고친다. 테이블 구조는 바뀌지 않는다.
--
-- **앞 마이그레이션(20260731075406_init_todo_entities)이 넣은 코멘트가 지금은 위험한
-- 방향을 지시한다.** 그 문장은 "todo_template 에서 가져와 채우며"였고, 그 시점에는
-- 맞는 판단이었다 — 저장 경로가 소유자를 검사하지 않았고 값이 어긋나면 (todo_id,
-- user_id) 를 묶은 외래키가 거절하는 것이 전부였으므로, 정의에서 복제하는 것이 어긋난
-- 값을 만들지 않는 방법이었다.
--
-- 그 뒤 저장 경로(TodoHistoriesRepository.upsertForHistoriedOn)가 (todo_id, user_id) 로
-- 정의를 찾아 **소유자를 검사하게** 됐고, 그러면서 이 값의 옳은 출처가 뒤집혔다.
-- 요청자의 id 를 넣어야 검사가 성립한다. 소유자로 좁히지 않고 읽은 정의에서 복제하면
-- 그 정의와 자기 자신을 비교하는 동어반복이 되어 아무것도 거르지 못하고, **그 경로에서는
-- 묶은 외래키도 통과한다** — 값이 정의와 일치하기 때문이고, 요청자가 아닌 제3자의 것으로
-- 일치할 뿐이다. 두 겹이 모두 뚫리는 유일한 경로다.
--
-- 이력에 과거가 남는 것은 결함이 아니므로 **앞 마이그레이션은 편집하지 않았다.** 이미
-- 적용된 파일을 고치면 체크섬이 어긋나 "modified after it was applied" 가 된다. 오도를
-- 막는 것은 현재 상태이고, 이 파일이 그것을 갱신한다.
--
-- **이 파일은 코멘트 한 줄만 바꾼다.** 행 단위 접근 제어(Row Level Security) 블록과
-- 나머지 코멘트 블록을 여기에 옮겨 붙이지 않는다 — 그 규칙은 마이그레이션을 **다시 뽑을
-- 때** 적용되고 이것은 앞 마이그레이션에 더하는 증분이다.
--
-- prisma migrate diff 는 코멘트를 비교하지 않으므로 --create-only 로 빈 파일을 만든 뒤
-- 손으로 채웠다. test/schema-guard.e2e-spec.ts 도 코멘트의 **존재**만 보고 내용을
-- 비교하지 않으므로, 이 종류의 어긋남을 잡아 주는 자동 관문은 저장소에 없다.

COMMENT ON COLUMN "todo_history"."user_id" IS '기록의 소유자 (app_user) 의 id. 요청자의 id 를 넣는다 - 저장 경로가 (todo_id, user_id) 로 정의를 찾아 소유자를 검사하기 때문이다. 소유자로 좁히지 않고 읽은 정의에서 복제하면 그 검사와 (todo_id, user_id) 를 묶은 외래키를 둘 다 통과해 남의 할 일에 기록이 쓰인다. 상세는 src/todos/todo-histories.repository.ts 의 TodoHistorySnapshot 주석에 있다';
